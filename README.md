TODO: ScreenHost but C++ server (sends efficient hi res live stream of screen and receives actions (clicks/hovers with position or keyboard strokes), on either TCP or UDP as chosen) and tauri client (just a black screen)
